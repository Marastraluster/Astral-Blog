### 第一个C语言程序：Hello,World!
```js{4}
#include <stdio.h>
int main(void){
    printf("Hello,World!");
    return 0;
}
```

这段代码中最重要的便是include关键字，它让我们把C语言的标准库stdio.h头文件给应用到这个C语言程序中，接着我们使用main函数进行我们的第一个程序工作printf函数来输出我们的 Hello,World! 从而达到我们本次程序的目的。
***